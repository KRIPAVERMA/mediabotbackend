# Exclude Flutter app — only the Node.js backend is deployed
mediabot_app/
*.apk

# Exclude build artifacts and test files
node_modules/
test_smtp.js

# Exclude local DB (Railway will create its own)
db/*.db
db/*.db-wal
db/*.db-shm

# Exclude large media files
downloads/*.mp3
downloads/*.mp4
downloads/*.webm
downloads/*.m4a
